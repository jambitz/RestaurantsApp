import {StyleSheet} from 'react-native';
import {COLORS} from '@constants';

const styles = StyleSheet.create({
  container: {
    paddingBottom: 3,
    alignSelf: 'center',
    marginLeft: '2%',
  },
  icon: {
    color: COLORS.WHITE,
  },
});

export {styles};
